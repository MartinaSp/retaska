{% extends 'base.html.twig' %}

{% block title %}Produkty: {{ controller_name }}!{% endblock %}

{% block body %}

      <div class="jumbotron">
        <div class="row">
        
        {% for product in products %}
          <div class="col-md-4">
            <h2> <a class="link" href="{{ path('product_detail') }}">{{product.name}} <span class="sr-only">(current)</span></a> </h2>
            <p>{{product.price}}</p>
            <p><a class="btn btn-secondary" href="#" role="button">Objednat </a></p>
          </div>
      
        {% endfor %}
        
        </div>

        <hr>

      </div>


{% endblock %}
